# 模型发布权利提示

本地包中的 7 个 `.pt` / `.pth` 权重来自当前项目文件。这个本地整理动作不改变这些权重原有的著作权、数据许可或第三方模型许可。

在将权重公开上传到 GitHub 或 GitHub Releases 前，仓库所有者需要确认：

1. 训练数据允许生成并公开分发这些权重；
2. 使用的基础架构及训练代码许可允许再分发；
3. 项目拥有公开这 7 个权重文件的授权；
4. 仓库中附上最终选择的项目许可证和必要的第三方声明。

在这些事项确认前，本目录应视为本地测试成品，而不是已获授权的公开发行物。
